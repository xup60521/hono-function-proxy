[build]
  command = "bun run build"
  publish = "dist" # If you have static files, otherwise just a placeholder

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200