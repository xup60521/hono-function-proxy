{
  "name": "hono-function-proxy",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
      "build": "mkdir -p dist netlify/functions && bun build ./src/index.ts --outdir ./netlify/functions --target node --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.11.4"
  }
}
